<Window x:Class="MovieEditor.Views.TimeTrimWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MovieEditor"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:behavior="clr-namespace:MovieEditor.Views.Behavior"
        mc:Ignorable="d"
        Title="時間範囲指定" Height="100" Width="500"
        Topmost="True"
        ui:WindowHelper.UseModernWindowStyle="True">

    <ui:SimpleStackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <Label Content="開始：" 
                VerticalAlignment="Center" HorizontalAlignment="Right"
                Grid.Column="0" />
            <TextBox Text="{Binding TrimStart}"
                Grid.Column="1" />
            <Label Content="終了：" 
                VerticalAlignment="Center" HorizontalAlignment="Right"
                Grid.Column="2" />
            <TextBox Text="{Binding TrimEnd}"
                Grid.Column="3" />
            <Button Command="{Binding EnterCommand}"
                Content="決定"
                HorizontalAlignment="Center"
                Grid.Column="4" />
        </Grid>
    </ui:SimpleStackPanel>

</Window>